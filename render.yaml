# =========================================================
# Render Deployment Blueprint — Web Service Version
# =========================================================
services:
  - type: web
    name: bayport-scheduler
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node scheduler.js
    envVars:
      - key: PAYEE_API
        sync: false
      - key: CALLLIST_API
        sync: false
